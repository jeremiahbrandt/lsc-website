import Navigation from "./Navigation";

const Header = () => {
    return (
        <header>
            <img src={"vercel.svg"} alt={"logo"} />
            <Navigation />
        </header>
    )
}

export default Header